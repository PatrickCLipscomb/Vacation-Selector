$(function() {
  $('form#blanks').submit(function(event) {
    event.preventDefault();
    $('#home').hide();
    $('#personal').show();

  });

  $('form#boxes').submit(function(event) {
    event.preventDefault();
    $('#personal').hide();
    var selection = 
    if ()
  }

});
